---
interface Props {
  class?: string;
  id?: string;
}

const { class: className, ...rest } = Astro.props;
---

<section class={`py-22 ${className ?? ''}`} {...rest}>
  <div class="container mx-auto px-4">
    <slot />
  </div>
</section>